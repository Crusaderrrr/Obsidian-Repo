#JavaScript 

==Операнды== это аргументы ==a== = ==10==

Арифметические:
`+, -, * , /`

Сравнения: 
`===, !==, <=, >= `

Логические:
`!(not), &&(and), ||(or)`

Присваивания:
`=` - `let c = a + b`

Текстовые:
```JS
typeof (проверить тип значения)
instanceof (проверить принадлежность классу)
new 
delete (удалить свойство)
```

Запятая:
`let a, b`

Унарные операторы (всегда один аргумент):
`a++` a + 1
`+a` конвертация строки в число
`delete` 
`typeof a` 
`new Object()` создать новый экземпляр

Инфиксная запись:
Оператор между операндами 
```JS
a = true
a + b
a += 5
a || b
a > b
```

Префиксная запись:
Оператор перед операндом
 ```JS
 ++a 
 delete obj.a
 typeof a
```

Постфиксная запись:
Оператор после операнда 
```JS
a++
myFunction()
```


### ... operator
The **...** operator has two uses:
- **Spread Operator**: Expands elements (e.g., copying arrays/objects).
	- Its like `array1.extend(array2)` in Python
- **Rest Operator**: Collects remaining elements into a single variable.
	- Used in functions as `*args` in Python
- Also can be used to fill other **object** with some information and change particular variable 
	- Same with **arrays**

```JavaScript
// Spread operator (copying and merging)
const arr1 = [1, 2];
const arr2 = [...arr1, 3, 4];
console.log(arr2); // [1, 2, 3, 4]

const obj1 = { name: 'Alice' };
const obj2 = { ...obj1, age: 25 };
console.log(obj2); // { name: 'Alice', age: 25 }

// Rest operator (collecting arguments)
function sum(...numbers) {
  return numbers.reduce((total, num) => total + num, 0);
}
console.log(sum(1, 2, 3, 4)); // 10

const person = {
	name: 'Nikita',
	age: 19,
	isMaried: false,
};

const person2 = {...person1, name: 'Jack'}
console.log(person2) // {name: 'Jack', age: 19, isMaried: false}
```
