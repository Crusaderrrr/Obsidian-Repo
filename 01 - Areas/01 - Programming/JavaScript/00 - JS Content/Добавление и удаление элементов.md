#JavaScript

Добавить:
- `myDict.newKey = newValue`
- `myDict['newKey'] = newValue`

Удалить:
- `delete myDict.value`

### *Сокращённая запись свойств объекта:*

```JS
const name = 'Nikita'
const job = 'undefined'

const portfolio = {
job, 
name,
dolboeb: true
}
```

