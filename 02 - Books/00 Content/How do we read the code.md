The code of the class/script/etc. should be structured the way, so that **we can read it as a newspaper**.

Meaning that for examples in classes the order would be like that:
1. declaration
2. fields 
3. public function
	- IMPORTANT: private function used by public function